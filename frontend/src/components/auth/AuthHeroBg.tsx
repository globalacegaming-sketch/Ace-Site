import { CosmicPageBg } from '../cosmic/CosmicPageBg';

/** Auth screens use the shared cosmic background (auth variant). */
export function AuthHeroBg() {
  return <CosmicPageBg variant="auth" />;
}
