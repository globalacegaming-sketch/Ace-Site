# Frontend Environment Variables
# Copy this file to .env.local and update the values

# Backend API URL (update with your Render backend URL)
VITE_API_BASE_URL=https://your-backend-url.onrender.com/api

# Games API URL (specific endpoint)
VITE_GAMES_API_URL=https://your-backend-url.onrender.com/api/games/fortune-panda

# WebSocket URL (update with your Render backend URL)
VITE_WS_URL=wss://your-backend-url.onrender.com

# Tawk.to Secret Key (optional - for HMAC-SHA256 hash generation)
# Get this from your Tawk.to dashboard: Settings > Channels > Chat Widget > Security
# If not provided, will use base64 encoding (less secure)
# IMPORTANT: For proper user identification, you should use HMAC-SHA256 with a secret key
VITE_TAWK_TO_SECRET_KEY=your-tawk-to-secret-key-here